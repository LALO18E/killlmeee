/* css/styles.css */

/* 1. FONDO GENERAL (El azul de la captura) */
body {
    /* Degradado vertical suave de azul claro a un azul un poco más fuerte */
    background: linear-gradient(180deg, #e3f2fd 0%, #bbdefb 100%) !important;
    background-attachment: fixed;
    min-height: 100vh;
    margin: 0;
    padding-bottom: 50px; /* Espacio para el botón flotante */
}

/* 2. BARRA DE NAVEGACIÓN */
nav {
    box-shadow: 0 2px 5px rgba(0,0,0,0.05); /* Sombra sutil */
}
nav .brand-logo {
    font-weight: bold;
    color: #333 !important;
}

/* 3. PANEL DE CONTROL (El cuadro verde de la captura) */
.header-card {
    /* Degradado Verde Intenso */
    background: linear-gradient(45deg, #00c853 0%, #69f0ae 100%) !important;
    color: white;
    padding: 25px 30px;
    border-radius: 4px 4px 0 0; /* Bordes redondeados solo arriba */
}
.header-card h4 {
    margin: 0;
    font-size: 1.8rem;
    display: flex;
    align-items: center;
}
.header-card p {
    margin: 5px 0 0 0;
    opacity: 0.9;
    font-weight: 300;
}

/* Contenedor blanco del panel */
#adminPanelContainer .card {
    border-radius: 4px;
    border: none;
    box-shadow: 0 4px 15px rgba(0,0,0,0.1);
}

/* 4. TARJETAS DE PRODUCTOS */
.card {
    border-radius: 12px;
    border: none;
    box-shadow: 0 5px 15px rgba(0,0,0,0.05);
    transition: transform 0.3s ease;
    background: white;
}
.card:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 20px rgba(0,0,0,0.1);
}
.card-image {
    border-radius: 12px 12px 0 0;
    overflow: hidden;
    height: 200px;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative; /* Para posicionar los botones flotantes */
}
.card-image img {
    width: 100%;
    height: 100%;
    object-fit: cover; /* Importante para que se vea bien */
}

/* Botones circulares de Editar/Borrar sobre la imagen */
.admin-actions {
    position: absolute;
    top: 10px;
    right: 10px;
    display: flex;
    gap: 8px;
    z-index: 10;
}
.admin-actions .btn-floating {
    width: 35px;
    height: 35px;
    line-height: 35px;
}
.admin-actions .btn-floating i {
    font-size: 1.2rem;
    line-height: 35px;
}

/* 5. CARRITO Y MODALES */
.cart-fab {
    bottom: 30px; right: 30px; z-index: 999;
}
.loading-overlay {
    position: fixed; top: 0; left: 0; width: 100%; height: 100%;
    background: rgba(255,255,255,0.9); z-index: 9999;
    display: flex; flex-direction: column; justify-content: center; align-items: center;
}

/* Corrección imágenes carrito */
.cart-item img { width: 50px !important; height: 50px !important; object-fit: cover; border-radius: 4px; margin-right: 10px; }
.cart-item { display: flex; align-items: center; padding: 10px 0; border-bottom: 1px solid #eee; }